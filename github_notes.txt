Open virtual environment with .\venv\Scripts\activate

Committing:
git add -A
git commit -m "Notes"
git push

Getting the other person's stuff:
git pull

Leave virtual environment with deactivate

Every time you open the code and want to start working:

enter virtual environment
git pull
pip install -r requirements.txt
